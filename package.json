{
  "name": "your-package-name",
  "version": "your-package-version",
  "dependencies": {
    // other dependencies
    // '@next/swc-android-arm64' removed
  },
  "overrides": {
    // other overrides
    // '@next/swc-android-arm64' removed
  }
}